name: chat_gpt_task
description: "A tqnia task"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^6.0.0
  flutter_screenutil: ^5.9.0
  flutter_svg: ^2.0.7
  smooth_page_indicator: ^1.1.0
  shimmer: ^3.0.0
  http: ^1.1.0
  provider: ^6.0.5
  animated_text_kit: ^4.2.2
  flutter_spinkit: ^5.2.0
  url_launcher: ^6.2.5


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:


  uses-material-design: true
  assets:
    - assets/images/
    - assets/

flutter_assets:
  assets_path: assets/images/
  output_path: lib/
  filename: image_assets.dart
  field_prefix: